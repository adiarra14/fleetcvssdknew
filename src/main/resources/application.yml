spring:
  application:
    name: lock-sdk-demo
  datasource:
    url: jdbc:postgresql://localhost:6078/balises_db
    username: ynnov
    password: Ynnov2025Fleet
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

mvgateway:
  servers:
    - port: 8910
      encrypt: true
      name: maxvision   # Do not modify
      host: 0.0.0.0
      ssl:
        enabled: false
      netty:
        boss-count: 1
        worker-count: 4
        keep-alive: true
        backlog: 100

logging:
  config: classpath:logback-spring.xml
  level:
    com.maxvision: DEBUG
    org.springframework: INFO 