spring:
  application:
    name: lock-sdk-demo
  datasource:
    url: jdbc:postgresql://localhost:5432/maxvision_lock_db
    username: ynnov
    password: Ynn0v@2025#Fleet
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

mvgateway:
  servers:
    - port: 8910
      encrypt: true
      name: maxvision   # Do not modify

logging:
  config: classpath:logback-spring.xml 